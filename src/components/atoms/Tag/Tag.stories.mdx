import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, label, boolean, select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { Padding } from "helpers/Display";
import Grid from "layout/Grid";
import Bar from "layout/Bar";
import Tag from "atoms/Tag";
import Icon from "atoms/Icon";

<Meta title="Atoms|Tag" component={Tag} />

# Tag

### Tags are used for items that need to be labeled, categorized, or organized using keywords that describe them.

<Preview withToolbar>
  <Story name="Standard">
    <Grid columns="4">
      <Tag label="Tag" />
    </Grid>
  </Story>
</Preview>

# Usage

If you need to represent the Taging between two states or on-off state.

The difference between Tag and Checkbox is that Tag will trigger a state change directly when you toggle it, while Checkbox is generally used for state marking, which should work in conjunction with submit operation.

A Tag is typically utilized in the following areas:

- Input Forms
- Filters
- Settings

Tags may appear in groups. When a Tag group is necessary use the TagGroup wrapper from the Tag directory to control the Tag layout.

# Guidelines

- **Opposing Options:**
  Typically Tags would be used where the options are very different from each other so no user would ever consider selecting more than one option. If a user selects Option A, Option B should not be desirable to them.
- **Clear:**
  A Tag’s action and state should be clear.
- **Be Brief**
  Tag labels should be short and to the point. They should be one or two words. A label should never be a sentence.

### Do & Don't

<Preview>
  <Story name="Usage">
    <React.Fragment>
      <Bar
        padding="2x"
        contentAlign="center"
        left={{
          content: <Icon icon="check" variant="success" size="lg" />,
          width: "5%",
        }}
        center={{
          content: <Tag label="Brief Label" />,
          align: "left",
        }}
      />
      <Bar
        padding="2x"
        contentAlign="center"
        left={{
          content: <Icon icon="close" variant="alert" size="lg" />,
          width: "5%",
        }}
        center={{
          content: <Tag label="Tag with a long label because you cannot be concise with your words." />,
          align: "left",
        }}
      />
    </React.Fragment>
  </Story>
</Preview>

# Anatomy

Tags consist of a circular input and a label. It is important that the label also has an htmlFor tag in order to link the Tag and be another way to select.

A Tag may appear to the left or right of it's corresponding label. A Tag may not appear above or below the label.

<!-- ## Behavior -->

# Examples

### Types

<Preview>
  <Story name="Types">
    <Grid columns="4">
      <Tag label="Info" variant="info" />
      <Tag label="Warning" variant="warning" />
      <Tag label="Success" variant="success" />
      <Tag label="Alert" variant="alert" />
    </Grid>
  </Story>
</Preview>


### Brands

<Preview withToolbar>
  <Story name="Brands">
    <Grid columns="4">
      <Tag label="Tag" brand="jobs" />
      <Tag label="Tag" brand="bi" />
      <Tag label="Tag" brand="research" />
      <Tag label="Tag" brand="broker" />
      <Tag label="Tag" brand="brand1" />
      <Tag label="Tag" brand="brand2" />
      <Tag label="Tag" brand="brand3" />
      <Tag label="Tag" brand="brand4" />
    </Grid>
  </Story>
</Preview>

### Props

<Props of={Tag} />
