import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, text, boolean, select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { Padding } from "helpers/Display";
import Grid from "layout/Grid";
import Loader from "./Loader.jsx";

<Meta title="Atoms|Loader" component={Loader} />

# Loader

#### A Loader is used to communicate to the user that a process is occuring and it might take some time to finish.

<Preview withToolbar>
  <Story name="Standard">
    <Loader/>
  </Story>
</Preview>



### Types


<Preview>
  <Story name="Types">
    <Grid columns="2">
      <Loader isLine />
       <Loader />
    </Grid>
  </Story>
</Preview>

## Props

<Props of={Loader} />
