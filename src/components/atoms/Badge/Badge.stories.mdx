import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, text, boolean, select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { Padding } from "helpers/Display";
import Grid from "layout/Grid";
import Card, { CardSection } from "layout/Card";
import Label from "atoms/Label";
import Badge from "atoms/Badge";

<Meta title="Atoms|Badge" component={Badge} />

# Badge

### Indicates association, status, and related information. Can be clickable, removable, or static. Small numerical value or status descriptor for UI elements.

<Preview withToolbar>
  <Story name="Standard">
    <Grid columns="4">
      <Card>
        <CardSection>
          <Badge label="1" />
          <Label text="Badge" />
        </CardSection>
      </Card>
    </Grid>
  </Story>
</Preview>

# Usage

Badges contain numeric values and indicate a running tally, such as the number of unread messages in a room. Use badges to label page items with a small amount of information that draws attention to new, updated or removed content. Badges are normally placed before or after the label of the thing they're quantifying. They can contain numbers, a max count (using a "+" sign to indicate a quantity greater than the displayed number), or a label to display status information.

- Use badges in conjunction with a single item so that there is no ambiguity around which item is indicated.
- Use tooltips on badges when they're useful to clarify meaning or enhance user understanding – for example to indicate units.
- Badges are for counts, so they should only contain integer numbers.
- Don't use badges to visually label UI objects; use tags instead.

# Examples

### Counts

A count can display up to a 4-character integer.

<Preview>
  <Story name="Counts">
    <Grid columns="4">
      <Card>
        <CardSection>
          <Badge label="1" />
          <Label text="1-Digit" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="21" />
          <Label text="2-Digit" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="321" />
          <Label text="3-Digit" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="4321" />
          <Label text="4-Digit" />
        </CardSection>
      </Card>
    </Grid>
  </Story>
</Preview>

### Max Count

If a count has a maximum limit, use a label to indicate a count that exceeds this limit.

<Preview>
  <Story name="Max Count">
    <Grid columns="4">
      <Card>
        <CardSection>
          <Badge label="9+" />
          <Label text="Max 10" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="99+" />
          <Label text="Max 100" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="999+" />
          <Label text="Max 1000" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="9999+" />
          <Label text="Max 10k" />
        </CardSection>
      </Card>
    </Grid>
  </Story>
</Preview>

### Status

Use a Label along with the 'type' prop to display status information.

<Preview>
  <Story name="Status">
    <Grid columns="4">
      <Card>
        <CardSection>
          <Badge label="Info" type="info" />
          <Label text="Info" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="Success" type="success" />
          <Label text="Success" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="Warning" type="warning" />
          <Label text="Warning" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="Alert" type="alert" />
          <Label text="Alert" />
        </CardSection>
      </Card>
    </Grid>
  </Story>
</Preview>

### Position

A Badge can be positioned on any corner of the element that is applied to.

<Preview>
  <Story name="Position">
    <Grid columns="4">
      <Card>
        <CardSection>
          <Badge label="1" position="topLeft" />
          <Label text="Top-Left" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="1" position="bottomLeft" />
          <Label text="Bottom-Left" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="1" position="topRight" />
          <Label text="Top-Right" />
        </CardSection>
      </Card>
      <Card>
        <CardSection>
          <Badge label="1" position="bottomRight" />
          <Label text="Bottom-Right" />
        </CardSection>
      </Card>
    </Grid>
  </Story>
</Preview>

## Props

<Props of={Badge} />
