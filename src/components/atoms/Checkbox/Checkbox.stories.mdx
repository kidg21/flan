import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, label, boolean, select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { Padding } from "helpers/Display";
import Grid from "layout/Grid";
import Bar from "blocks/Bar";
import Form, {Section} from "layout/Form";
import Checkbox, {CheckboxGroup} from "atoms/Checkbox";
import Icon from "atoms/Icon";

<Meta title="Work|Docs/Checkbox" component={Checkbox} />

# Checkbox

### Component Definition or description

<Preview withToolbar>
  <Story name="Standard">
    <Checkbox label="Checkbox" checked/>
  </Story>
</Preview>

# Usage
Checkboxs are used to select a single state from multiple options.

Use checkboxes to:

-Select one or multiple items from a list
-Present a list containing sub-selections
-Turn an option on or off in desktop environment

If a list consists of multiple options, avoid using switches. Use checkboxes instead because they take up less space.

Checkboxes are for making selections from a list. They let users select more than one item and allow for easy selection or deselection of all items with a parent checkbox.

Checkboxes allow the user to select one or more items from a set. Checkboxes can be used to turn an option on or off.

# Guidelines
- **Multi Select:**
   Using checkboxes to select multiple items in a list.
- **Identify State:**
  Using a checkbox to turn an option on or off
- **Not required**
  Checkboxes are typically considered optional selection, while radio is perceived as being a requirement.



# Anatomy
Checkboxs consist of a square input and a label. It is important that the label also has an htmlFor tag in order to link the Checkbox and be another way to select.

A Checkbox may appear to the left or right of it's corresponding label. A Checkbox may not appear above or below the label. 


# Examples

### States
We can drop it in a `Preview` to get a code snippet:
<Preview>
  <Story name="States">
    <Grid>
      <Checkbox label="Checked" checked/>
      <Checkbox label="Unchecked"  />
      <Checkbox label="Error" error />
      <Checkbox label="Disabled" disabled  />
    </Grid>
  </Story>
</Preview>

### Alignment
<Preview>
  <Story name="Alignment">
    <Grid>
      <Checkbox label="Left Aligned"  />
      <Checkbox label="Right Aligned" align="right" />
    </Grid>
  </Story>
</Preview>

### Group
<Preview>
  <Story name="Checkbox Group">
    <Grid>
    <Form>
<Section title="Checkbox Group (2 Columns)">
      <CheckboxGroup columns="2">
      <Checkbox 
          id="Checkbox-1"
    name="Checkbox-group"
    value="1"
    label="Checkbox 1"
    />
          <Checkbox 
          id="Checkbox-2"
    name="Checkbox-group"
    value="2"
    label="Checkbox 2"
    />
          <Checkbox 
          id="Checkbox-3"
    name="Checkbox-group"
    value="3"
    label="Checkbox 3"
    />
          <Checkbox 
          id="Checkbox-4"
    name="Checkbox-group"
    value="4"
    label="Checkbox 4"
    />
    </CheckboxGroup>
    </Section>
    </Form>
    </Grid>
  </Story>
</Preview>


### Props
<Props of={Checkbox} />

