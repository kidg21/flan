import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import Form from "layout/Form";
import Checkbox from "atoms/Checkbox";

<Meta title="Work|Docs/CheckboxDocs (MDX)" component={Checkbox} />

# Checkbox

With `MDX` we can define a story for `Checkbox` right in the middle of our markdown documentation.

<Preview>
  <Story name="MDX Format">
    <Form>
      <Checkbox id="cbox1" label="Checkbox 1" />
    </Form>
  </Story>
</Preview>
