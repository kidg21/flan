import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import * as stories from "./Button_CSF_Stories.stories.js";
import Grid from "layout/Grid";
import Button from "atoms/Button";

<Meta
  title="Work|Docs/ButtonDocs (MDX Docs with CSF Stories)"
  component={Button}
/>

# Button

<Story name="Standard">{stories.Standard()}</Story>

With `MDX` we can define a story for `Button` right in the middle of our markdown documentation.

## Props

<Props of={Button} />

## Stories

### Type (CSF)

We can drop it in a `Preview` to get a code snippet:

Imported as CSF - irrelevant code preview

<Preview>
  <Story name="Type (CSF)">{stories.Type()}</Story>
</Preview>

### Type (MDX)

Written in MDX - useful code preview

<Preview>
  <Story name="Type (MDX)">
    <Grid>
      <Button label="Standard Button" />
      <Button label="Solid Button" type="solid" />
      <Button label="Inline Button" type="inline" />
      <Button label="Underlined Button" type="underlined" />
    </Grid>
  </Story>
</Preview>

We can drop it in a `Knobs` to get a code snippet:

<Preview>
  <Story name="Knobs">{stories.Knobs()}</Story>
</Preview>
