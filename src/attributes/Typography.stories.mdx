import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, text, boolean, select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { Padding } from "helpers/Display";
import Grid from "layout/Grid";
import Card, { CardSection } from "layout/Card";
import Text, {Title, Link, Label} from "base/Typography";

<Meta title="Application Guidelines|Typography" component={Text} />

# Badge

### Indicates association, status, and related information. Can be clickable, removable, or static. Small numerical value or status descriptor for UI elements.

<Preview withToolbar>
  <Story name="Standard">
    <Grid columns="1">
     <Title size="6x" text="6x Title"/>
     <Title size="5x" text="5x Title"/>
     <Title size="4x" text="4x Title"/>
     <Title size="3x" text="3x Title"/>
     <Title size="2x" text="2x Title"/>
     <Title size="1x" text="1x Title"/>
     <Text size="4x" text="4x Text"/>
     <Text size="3x" text="3x Text"/>
     <Text size="2x" text="2x Text"/>
     <Text size="1x" text="1x Text"/>
     <Label size="4x" text="Label"/>
     <Label size="3x" text="Label"/>
     <Label size="2x" text="Label"/>
     <Label size="1x" text="Label"/>
     <Link text="this is a link"/>
    </Grid>
  </Story>
</Preview>


## Props

<Props of={Text} />
